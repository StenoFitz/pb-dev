---
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="./favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} >
    <title>Purebeing | Coming Soon</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;500;700&display=swap" rel="stylesheet">

    <style>
      :root {
        --primary: #2d8cff;
        --bg-color: #f8fbfd; /* Very soft blue-white */
        --text-main: #2d3436;
        --text-muted: #636e72;
      }

      body {
        font-family: 'Inter', sans-serif;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        margin: 0;
        background: linear-gradient(135deg, #eef2f3 0%, #d5e1df 100%);
        color: var(--text-main);
        text-align: center;
      }

      .container {
        padding: 3rem 2rem;
        max-width: 450px;
        width: 90%;
        border-radius: 24px;
        background: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(10px); /* Modern "glass" effect */
        box-shadow: 0 20px 40px rgba(0,0,0,0.05);
      }

      h1 {
        font-weight: 700;
        font-size: 2rem;
        margin-bottom: 0.5rem;
        letter-spacing: -0.02em;
      }

      .status-badge {
        display: inline-block;
        padding: 4px 12px;
        background: #fff3cd;
        color: #856404;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: bold;
        text-transform: uppercase;
        margin-bottom: 1rem;
      }

      p {
        line-height: 1.6;
        color: var(--text-muted);
        margin: 0.5rem 0 1.5rem;
      }

      .zoom-button {
        display: block;
        padding: 16px 24px;
        background-color: var(--primary);
        color: white;
        text-decoration: none;
        border-radius: 12px;
        font-weight: 600;
        transition: transform 0.2s, box-shadow 0.2s;
        box-shadow: 0 4px 14px rgba(45, 140, 255, 0.4);
      }

      .zoom-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(45, 140, 255, 0.6);
        background-color: #1a7ae5;
      }

      .footer-note {
        font-size: 0.85rem;
        margin-top: 2rem;
        opacity: 0.6;
      }
    </style>

</head>
  <body>
      <body>
    <div class="container">
      <span class="status-badge">Website Coming Soon</span>
      <h1>Pure Being</h1>
      <p>We're currently crafting a new digital space for mindfulness.</p>
      
      <hr style="border: 0; border-top: 1px solid #eee; margin: 2rem 0;" />

      <p style="font-weight: 500; color: var(--text-main); margin-bottom: 1.5rem;">
        Join us for Monday Meditation:
      </p>
      
      <a href="https://us02web.zoom.us/j/88684664522?pwd=eFhHMUhmeFBEYkJyTERpSzBMdVdKdz09" class="zoom-button">
        Join Zoom Meeting
      </a>

      <div class="footer-note" style="margin-top: 2.5rem;">
       <p>See you there at 9:00 PM Irish Time </p> 
      </div>
    </div>
  </body>
  </body>
</html>